import React from "react"

import { Post } from "../../types"
import { usePostList } from "../../hooks"

const PostList = () => {
	return (
		<div>
			<h1>Post List</h1>
		</div>
	)
}

export default PostList